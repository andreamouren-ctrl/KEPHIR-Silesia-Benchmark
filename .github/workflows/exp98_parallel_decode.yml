name: KEPHIR 2 EXP-98 Parallel Decode Scaling

on:
  workflow_dispatch:
  push:
    branches:
      - development/kephir-2-core
    paths:
      - "research/benchmarks/exp98_parallel_decode.py"
      - ".github/workflows/exp98_parallel_decode.yml"

jobs:
  benchmark:
    runs-on: ubuntu-latest
    timeout-minutes: 240

    steps:
      - uses: actions/checkout@v4

      - name: Build native KEPHIR
        run: |
          cmake -S src/kephir2 -B build/kephir2 -DCMAKE_BUILD_TYPE=Release
          cmake --build build/kephir2 --config Release --parallel

      - name: Native test suite
        run: ctest --test-dir build/kephir2 --output-on-failure

      - name: Download canonical Silesia
        run: |
          mkdir -p corpora/silesia
          curl -L --fail --retry 4 -o /tmp/silesia.zip             https://sun.aei.polsl.pl/~sdeor/corpus/silesia.zip
          echo "c240c17d6805fb8a0bde763f1b94cd99  /tmp/silesia.zip" | md5sum -c -
          unzip -q /tmp/silesia.zip -d corpora/silesia
          test "$(find corpora/silesia -type f -printf '%s\n' | awk '{s+=$1} END {print s}')" -eq 211938580

      - name: Run EXP-98
        run: |
          python3 research/benchmarks/exp98_parallel_decode.py             build/kephir2/kephir2_native_k75_cli

      - name: Validate result
        run: |
          python3 - <<'PY'
          import json
          d=json.load(open("exp98_results.json"))
          assert d["raw_bytes"] == 211938580
          assert d["archive_bytes"] > 0
          assert len(d["rows"]) == 5
          assert all(r["sha_all_pass"] for r in d["rows"])
          best=max(r["decode_MBps"] for r in d["rows"])
          base=d["rows"][0]["decode_MBps"]
          assert best > base
          print("EXP98_VALIDATION_PASS", "BASE",base,"BEST",best)
          PY

      - uses: actions/upload-artifact@v4
        with:
          name: exp98-parallel-decode-results
          path: exp98_results.json
